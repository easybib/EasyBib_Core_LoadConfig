language: php
before_install:
  - phpenv config-rm xdebug.ini
  - pecl install -f memcache-2.2.6
  - pecl install -f apc-3.1.9
install:
  - php --ini
  - php -m
  - php composer.phar install
php:
  - 5.6
